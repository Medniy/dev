---
driver:
  name: docker

platforms:
  - name: instance
    image: "ubuntu:20.04"
    pre_build_image: true

provisioner:
  name: ansible
  playbooks:
    converge: converge.yml

verifier:
  name: ansible

